{>"layouts/master" /}

{<body}
	{#books}
	<div class="medium-3 columns book">
		<img src="/img/{.cover}" alt="" class="bookImage">
		<h4>{.title}</h4>
		<p>{.truncText}</p>
		<div class="price">Buy it for <span>{.price}</span></div>
		<br>
		<a class="button small" href="/books/details/{._id}">Book Details</a>
	</div>
	{/books}


{/body}
